{
  "name": "package-based",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "test": "nx run-many -t build && cat packages/test/output.txt"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "nx": "^16.7.4",
    "nx-cloud": "latest"
  },
  "workspaces": [
    "packages/*"
  ]
}
